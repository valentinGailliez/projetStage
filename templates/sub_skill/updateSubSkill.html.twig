{% extends 'base.html.twig' %}

{% block title %}
	Gestion skill et subskill
{% endblock %}
{% block body %}
	<h1>
		Sous-Compétence</h1>
	<div class="container">
		<div class="row">
			{{ form_start(form, {
                'attr': {
                    'novalidate': 'novalidate',
                    'class': 'col-lg-5' 
                    }
                })
            }}


			{{ form_widget(form.number)}}

			<button type="submit" class="btn btn-primary">
				mettre à jour la compétence</button>
			{{ form_end(form)}}


		</div>
	</div>
{% endblock %}